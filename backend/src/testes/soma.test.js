const userModel = require('../server');
test("Retornar soma", () => {    
    expect(1+1).toBe(2);
})

test("Retorna divisão", () => {
    expect(10/5).toBe(2);
})

// test('Listar usuários: ', () =>{
//     const user = new server;
//     const lista = 
// });